const NoPage = () => {
  return (
    <div
      style={{
        fontSize: "5rem",
        fontWeight: "bold",
        textAlign: "center",
        justifyContent: "center",
      }}>
      Not found page
    </div>
  );
};

export default NoPage;
